"""
Interview scheduling API routes.

Employer: availability, slots, send offer, confirm, cancel (Step 7).
Candidate: view slots, pick slot, cancel (Step 8).
"""

# from fastapi import APIRouter
# router = APIRouter(prefix="/interview-scheduling", tags=["interview-scheduling"])
